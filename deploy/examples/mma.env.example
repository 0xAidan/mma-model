# EXAMPLE ONLY — NOT INSTALLED (DWCS-004)
# Copy to /etc/mma-model/mma.env on the host in a later ticket.
# Required ownership/mode: root:root mode 0600.
# Never embed live API keys in git.

# Odds / stats providers (fill in later tickets; leave empty here)
THE_ODDS_API_KEY=PLACEHOLDER
BALLDONTLIE_API_KEY=PLACEHOLDER

# Runtime paths inside the worker container mounts
MMA_DATA_DIR=/data
MMA_PUBLIC_DIR=/public

# Dashboard basic_auth is configured in Caddy, not here.
